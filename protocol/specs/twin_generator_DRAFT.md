# Prior-blocked twin generator — DRAFT / UNFROZEN

Randomization must preserve semantic type/granularity and avoid natural-value collisions.

- entity -> frozen fictitious-entity support;
- scope -> scope support;
- annual period -> annual period support;
- quarterly period -> quarterly period support;
- numeric -> unit-preserving eligible numeric subtype/support;
- provenance -> provenance support.

All occurrences of the same mapped atom must receive the same replacement.

The operative chance reference should be the empirical no-evidence baseline unless uniform target sampling and constrained response support make an analytic `1/K` statement exact.
