# Availability editor — DRAFT / UNFROZEN

The natural message is never edited on the observed side.

For focal atom `z`:

- if `T_z=1`, construct only `M^{-z}`;
- if `T_z=0`, construct only `M^{+z}`.

## Non-focal invariance

The editor operates on the **relay message**, not source-document offsets. Every canonical-equivalent relay occurrence of the focal value must be located according to the frozen matcher/editor rules. Non-focal content must remain byte-identical except for predeclared whitespace normalization.

## Audits

- deletion/insertion mechanical sham controls are separate;
- coherence failures are logged, never silently replaced by a new focal atom after observing `T`;
- exclusion/failure rates are reported by role, `T`, and mechanism;
- the final preregistration must freeze overall and differential intervention-failure caps.
