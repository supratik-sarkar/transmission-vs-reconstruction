# Matched-skeleton context construction — DRAFT / UNFROZEN

Experiment B requires a source-linked context tuple rather than arbitrary document-level atom combinations.

## Candidate context tuple

The final protocol must freeze a deterministic local-context rule, e.g. same sentence/clause or fixed source window. A tuple may contain only Tier-1 atoms linked by that source context.

For focal `z`:

1. select a pre-treatment Experiment-B eligible tuple;
2. hold non-focal role/slot presence, ordering, punctuation, and rendering structure fixed;
3. omit the focal target in BOTH arms;
4. natural arm uses natural identity/value mapping;
5. prior-blocked arm applies the frozen controlled mapping;
6. no relay-policy regeneration is used to define the primary contrast.

If no valid tuple exists under the frozen rule, the atom is Experiment-B ineligible and must not be replaced after observing outcomes.
