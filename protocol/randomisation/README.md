# Randomisation supports — DRAFT

The final supports must be frozen and hashed. Natural-value collisions must be excluded without silently claiming `K-1` when the natural value was never in the support.

The empirical no-evidence baseline is the preferred operative chance reference unless uniformity/constrained-response assumptions are explicitly satisfied.
